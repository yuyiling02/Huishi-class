# 数智课堂演示视频录制稿

建议时长：5 分钟左右  
录制方式：边操作系统边配旁白  
演示主线：从首页进入平台，展示 3D 教具资源、多智能体自动演示、语音控制、手势交互和知识讲解生成。

---

## 一、开场：项目定位（约 30 秒）

### 画面操作
打开系统首页，停留在“数智课堂”首屏，展示 3D 科技背景与“立即体验”入口。

### 旁白
大家好，今天演示的是“数智课堂”，这是一套面向课堂教学场景的 AI 驱动 3D 交互式教具平台。

传统课堂里，很多知识点只能通过 PPT、图片或者视频来展示，学生看到的是二维结果，很难真正理解结构关系。数智课堂希望解决的就是这个问题：让抽象知识点变成可以旋转、拆解、放大、讲解和交互的三维模型。

系统目前集成了 3D 模型库、多智能体教学流程、语音意图识别和手势控制能力，教师可以用更自然的方式完成课堂演示。

---

## 二、进入平台：整体界面介绍（约 40 秒）

### 画面操作
点击“立即体验”，进入登录后的主界面。展示左侧资源与智能体面板、中间 3D 展示区、底部语音按钮、右上角全屏按钮。

### 旁白
进入平台后，主界面分为三个部分。

中间是 3D 教学展示区，所有模型都会在这里实时渲染。左侧是资源库和多智能体控制面板，教师可以按学科选择模型，也可以直接输入教学目标。底部提供语音控制入口，右上角支持全屏演示，方便在课堂大屏或录课环境中使用。

这个界面的设计目标是让教师少操作、多讲解。常用功能都围绕课堂演示组织，而不是像专业 3D 软件那样要求复杂的鼠标和键盘操作。

---

## 三、模型资源库演示（约 50 秒）

### 画面操作
展开左侧资源库，依次点击地理、生命科学、化学模型。例如先点击“地球内部结构”，再切换到“心脏模型1”或“金刚石晶胞”。

### 旁白
首先看一下 3D 教具资源库。

目前系统内置了多个教学模型，覆盖地理、生物和化学等常见课堂场景。比如地理课可以展示地球内部结构和地形地貌；生物课可以展示人体心脏和 HIV 病毒模型；化学课可以展示金刚石、NaCl 离子晶体、二氧化硅网络以及分子结构。

点击模型后，系统会直接加载到中间的 3D 展示区。教师可以通过旋转、缩放和全屏展示，引导学生从整体形态进入到局部结构。

这类 3D 教具特别适合解释空间关系明显、二维图不够直观的知识点，例如地球分层、心脏腔室、晶体结构和分子构型。

---

## 四、多智能体自动演示：核心功能（约 90 秒）

### 画面操作
切换到“多智能体平台”面板。输入或保留示例指令：“讲解地球内部结构，展示地壳、地幔、外核和内核的关系”。点击运行。录制智能体状态变化、模型加载、旋转、拆解、知识讲解生成。

### 旁白
接下来演示本系统的核心功能：多智能体自动教学流程。

这里我输入一个教学目标：“讲解地球内部结构，展示地壳、地幔、外核和内核的关系”。点击运行后，系统会启动三个智能体协同工作。

第一个是理解规划 Agent。它会先理解教师的教学需求，判断当前最适合使用哪个 3D 教具，并生成演示步骤。比如这个场景中，系统会自动匹配“地球内部结构”模型。

第二个是演示执行 Agent。它会把教学步骤转化为具体的模型操作，比如加载模型、自动旋转、放大重点区域、拆解内部结构，并把这些操作按顺序执行出来。

第三个是知识讲解 Agent。它会根据当前模型和教学目标，生成适合课堂讲解的知识内容。生成的文本会在左侧面板和展示区中同步出现，也可以配合语音播报使用。

这个流程的价值在于，教师不需要提前写好每一步操作，只要提出教学目标，系统就能完成“理解需求、规划流程、执行演示、生成讲解”的闭环。

---

## 五、语音控制演示（约 50 秒）

### 画面操作
加载一个模型后，点击底部语音按钮。依次说出：“放大一点”“旋转一下”“停止”“切换到心脏模型”“帮我讲解心脏结构”。如果现场语音环境不稳定，也可以口述说明并展示按钮位置。

### 旁白
除了点击操作，系统还支持语音意图识别。

对于简单控制命令，比如“放大一点”“缩小”“旋转一下”“停止”，系统会通过规则匹配直接执行，响应速度非常快。对于“切换到心脏模型”“打开地球内部结构”这类模型切换命令，系统会自动识别目标模型并完成加载。

如果教师说的是更复杂的教学任务，例如“帮我讲解心脏结构”，系统会把它识别为教学任务，并启动多智能体流程。

这里采用的是混合式意图识别架构：简单命令走本地规则，保证低延迟；复杂请求交给大模型理解，保证自然语言能力。这样既适合课堂上的即时控制，也适合较复杂的教学编排。

---

## 六、手势交互演示（约 50 秒）

### 画面操作
点击“启用手势捕捉”，展示摄像头预览。演示张开/握拳缩放、捏合拖拽、双指控制旋转。如果现场不方便开摄像头，可展示界面中的手势说明区域。

### 旁白
系统还集成了基于 MediaPipe 的手势识别能力。

开启手势捕捉后，教师可以通过手势控制 3D 模型。比如左手张开或闭合可以控制缩放，右手捏合可以拖拽模型部件，食指和中指并拢移动可以控制画面旋转。

这对于课堂演示很有意义。教师不必一直站在电脑前操作鼠标，而是可以面向学生，用手势直接控制模型，把讲解动作和三维展示结合起来。

在展示复杂结构时，手势交互也能增强课堂参与感，让学生更容易把注意力集中在模型变化和知识关系上。

---

## 七、结构图与知识讲解补充（约 35 秒）

### 画面操作
加载支持结构图的模型，例如心脏、HIV、地球内部结构或化学模型。点击左上角结构图放大；展示知识讲解面板。

### 旁白
除了 3D 模型本身，系统还会为部分模型配套结构示意图和知识讲解。

例如在展示心脏、HIV 病毒、地球内部结构或化学分子时，左上角会出现对应的结构图。教师可以点击放大，结合 3D 模型和二维结构图进行对照讲解。

这种组合可以降低学生的理解门槛：三维模型负责展示空间关系，结构图负责帮助学生抓住关键名称和层级。

---

## 八、技术路线与系统价值（约 35 秒）

### 画面操作
可以切回首页或展示架构图素材，例如 `output/01-overall-architecture.png`、`output/02-multi-agent-workflow.png`。

### 旁白
从技术上看，系统前端基于 React、TypeScript 和 Three.js 构建 3D 交互体验，手势识别使用 MediaPipe，语音识别基于浏览器 Web Speech API，智能体能力接入 DeepSeek 等大模型。

系统并不是单纯展示模型，而是把“教学目标理解、3D 操作执行、知识讲解生成”整合到同一个流程里。

它的核心价值可以概括为三点：第一，让抽象知识可视化；第二，让课堂操作更自然；第三，让 AI 从问答工具升级为可以参与教学流程的智能助教。

---

## 九、结尾总结（约 30 秒）

### 画面操作
回到 3D 展示区，全屏展示一个模型，例如地球内部结构或心脏模型，保持自动旋转。

### 旁白
以上就是数智课堂的核心演示。

它通过 3D 模型、多智能体 AI、语音控制和手势交互，把传统课堂中的抽象知识转化为可观察、可操作、可讲解的沉浸式教学内容。

未来，这套系统还可以继续扩展更多学科模型、课堂互动数据、学生端参与和 VR/AR 场景，为智慧课堂提供更完整的数字化教学支持。

我的演示到这里结束，谢谢大家。

---

## 3 分钟精简版结构

如果录制时间较短，可以按下面节奏压缩：

1. 开场 20 秒：介绍数智课堂解决抽象知识难理解的问题。
2. 界面 25 秒：展示首页、资源库、3D 展示区。
3. 模型 35 秒：快速切换地球、心脏、化学模型。
4. 多智能体 70 秒：重点演示输入教学目标、自动规划、加载、拆解、生成讲解。
5. 语音和手势 40 秒：展示“放大、旋转、停止”和手势控制。
6. 总结 30 秒：强调 3D 可视化、多模态交互和 AI 教学闭环。

---

## 录制提示

- 优先选择“地球内部结构”作为主演示模型，因为它有明显的分层和拆解效果。
- 如果语音识别现场不稳定，可以先录制画面，再后期配音。
- 手势演示建议只展示 1 到 2 个动作，避免因为环境光线或摄像头角度影响录制节奏。
- 多智能体运行时，镜头可以停留在左侧状态流和中间模型变化上，让观众看到系统不是只输出文字，而是在实际控制 3D 教具。
- 结尾最好用全屏模型自动旋转作为收束画面，视觉效果更完整。
